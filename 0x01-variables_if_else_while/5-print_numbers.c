#include <stdio.h>

/**
 * main-program entry point.
 * Return: 0 no errors, non zero if erro.
 */
int main(void)
{
	char i;

	for (i = '0'; <= '9'; i++)
	{
putchar(i);
}
putchar('\n');
return (0);
}

